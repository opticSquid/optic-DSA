{
  // debug configurations
  "version": "2.0.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Run C++ (clang + LLDB)",
      "program": "${fileDirname}/${fileBasenameNoExtension}",
      "args": [],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "C/C++: clang++ build active file"
    }
  ]
}
